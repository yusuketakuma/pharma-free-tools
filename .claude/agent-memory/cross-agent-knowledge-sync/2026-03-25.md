# Cross-Agent Knowledge Sync — 2026-03-25 06:50 JST

## 結論
直近のローカル実行ログから、再利用価値が高い知識は **OpenClaw の lane selection / fallback / queued execution 周りの回帰** が中心だった。
加えて、CareRoute-RX の探索では **想定パスに auth/login 実装が見つからない** という repo layout mismatch が出ている。

> 補足: `sessions_list` / `subagents` からは今回、他セッションの新規実行結果は取得できず、横断共有は `.claude/state/sessions` と参照先 output artifact に依存した。

## 今回共有すべき知識

### 1) queue_rebalance / lane runtime の回帰が再現している
反復して出ている失敗は以下。
- `test_acceptance_lane_selection_falls_back_to_cli_when_acp_unhealthy`
  - 期待: `selected_lane == "cli"`
  - 実際: `"acp"`
- `test_acceptance_lane_selection_waiting_capacity_when_all_lanes_unhealthy`
  - 期待: `execution_mode == "queued"`
  - 実際: `"claude_code"`
- `test_acceptance_cli_backend_safety_net_fallback_from_cli`
  - 期待: 両方失敗時に `cli_backend_safety_net` まで試行
  - 実際: `len(attempts) == 1`
- `step6-acp-mock-contract-*` のタスクで `execute_task.py` が exit status 20 を返すケースあり

### 2) これは単発ではなく、複数回の unittest 実行で同じ壊れ方をしている
- 18 tests の実行で 3 failures + 1 error
- 23 tests の実行で 2 failures + 1 error
- 失敗箇所はほぼ同じで、回帰ポイントが固定化している

### 3) CareRoute-RX の探索では、auth/login の実装位置が想定とずれている
探索コマンドは `find` / `ls` 中心で、`
- /Users/yusuke/.openclaw/workspace/projects/careroute-rx
- /Users/yusuke/careroute-rx
` の両方を見ているが、`src` や `apps/web/src/app/api/auth/login` など期待パスでは実装が見つからない。

## 再利用先エージェント

- **engineering-manager**: lane selection / fallback の設計回帰を優先的に切り分ける
- **qa-manager**: 上記 3 失敗を固定回帰テストとして保持し、再実行コストを最小化する
- **ops-automator**: all-lanes unhealthy 時は queued に倒す運用契約を確認する
- **backend-architect**: `execute_task.py` / lane runtime の遷移条件を修正対象として扱う
- **research-analyst**: CareRoute-RX の repo layout mismatch を前提に次の探索パスを再設計する
- **doc-editor**: lane runtime / fallback contract を runbook に落とす
- **dss-manager**: 失敗パターンを再配置判断の共通知識として反映する

## 重複回避示唆

- まず `queue_rebalance` と `lane_runtime` の既知失敗を確認してから再テストする
- 変更なしのまま broad unittest を回すのは重複コストが高い
- 先に以下の焦点テストだけを回すのがよい:
  - `test_acceptance_lane_selection_falls_back_to_cli_when_acp_unhealthy`
  - `test_acceptance_lane_selection_waiting_capacity_when_all_lanes_unhealthy`
  - `test_acceptance_cli_backend_safety_net_fallback_from_cli`
  - `test_acceptance_acp_adapter_mock_contract_preserved`
- CareRoute-RX は「auth/login がある前提」で探し直さず、現行 repo tree を先に確定する

## 成果物/共有メモ

- 共有メモ: `.claude/agent-memory/cross-agent-knowledge-sync/2026-03-25.md`
- 参照 source:
  - `.claude/state/sessions/session-1774164930.events.jsonl`
  - `.claude/state/sessions/session-1774165213.events.jsonl`
  - `.claude/state/sessions/session-1774165260.events.jsonl`
  - `.claude/state/sessions/session-1774181826.events.jsonl`
  - output artifacts under `/private/tmp/claude-501/-Users-yusuke--openclaw-workspace/.../*.output`

## 次アクション

1. lane selection / fallback の実装箇所を特定して、`acp -> cli -> cli_backend_safety_net -> queued` の遷移条件を再確認する
2. queue_rebalance の 3 失敗を固定回帰として再実行し、修正の前後差分を取る
3. CareRoute-RX は repo tree を先に確定し、auth 系の探索パスを再設計する
