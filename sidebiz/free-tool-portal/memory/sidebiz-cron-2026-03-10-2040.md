# 副業保守サイクル実行記録 - 2026-03-10 20:40 JST (sidebiz-30m-maintenance)

## status
- **alert**

## 監視結果

### 1) 起動/アクセス（sitemap対象25件）
- 到達性: **22件OK / 3件NG**

[ALERT] NG一覧:
- `https://yusuketakuma.github.io/pharma-medication-guidance/` → 404
- `https://yusuketakuma.github.io/pharma-medication-reminder/` → 404
- `https://yusuketakuma.github.io/pharma-renal-dose/` → 404

### 2) 主要機能スモーク
- `free-tool-portal/pharmacy-ai-readiness.html`：`renderQuestions`/`nextQuestion`/`showResult` が存在し、初期UI要素確認。
- `free-tool-medication-summary/index.html`：`generateSummary` とコピー系要素/ロジックが存在。
- `free-tool-homecare-scheduler/index.html`：`homecare-patients` キーの `localStorage` 保存・読込処理確認、`DOMContentLoaded` 起動経路確認。

### 3) 依存更新 / データ保存 / 認証
- 主要外部依存: GitHub Pages静的配信 + 個別CDN（`cdn.tailwindcss.com` は2ページのみ）。
- 認証/API: 今回対象HTML群でOAuth/トークン認証に依存する新規呼び出しは確認できず。GA IDはプレースホルダ（`G-XXXXXXXXXX`）のまま。
- データ保存: 本体で稼働確認したデータ永続化は`localStorage`（`homecare-patients`）のみで、今回の巡回で破損再現なし。

### 4) 全HTMLリンク監査（`https://*.github.io` 抽出）
- 対象URL（ユニーク）: **105件**
- 404/到達不可: **59件**

[ALERT] 404上位:
1. `https://yusuketakuma.github.io/pharma-medication-reminder/`（**20件**）
2. `https://yusuketakuma.github.io/pharma-medication-guidance/`（**18件**）
3. `https://yusuketakuma.github.io/pharma-renal-dose/`（**6件**）

加えて、`yusuke-ikitama.github.io/*` 系旧ドメイン参照と `free-tool-*` 系と `pharma-*` の混在が多数残存。

## 障害再現条件
1. 4043件の公開URLへ直接アクセス。
2. Portal/関連記事導線で上記3件がリンク先として設定されているカードや導線をクリック。
3. `yusuke-ikitama.github.io/*` 経由リンク（多くは `free-tool-*` 命名）を踏む。

## 影響範囲
- **高:** `pharma-medication-guidance` / `pharma-medication-reminder` / `pharma-renal-dose` は新規訪問導線として到達不可。
- **中:** Portal/記事内の関連記事導線でリンク切れが大量発生し、初回導線喪失・離脱リスク増。
- **低〜中:** 依存先URLの命名揺れ（`free-tool-*`/`pharma-*`/旧ドメイン）により監査と修正工数が増加。

## 学習ポイント（次サイクル改善）
- URL運用ルールが複数系統（`free-tool-*` と `pharma-*`、旧ドメイン）で分散すると、1件修正が数十件の導線エラーとして再発しやすい。
- 配信前の静的HTMLリンク監査（相対リンク＋絶対リンク）を自動化しないと、Portal OK でも記事内リンクが落ちた状態を見逃す。

## 次アクション
1. 30分以内: 3件主要404のURLを公開実体へ統一更新（`pharma-*` 正式公開先へ置換）。
2. 30分以内: `yusuke-ikitama.github.io/*` と混在URL一覧を抽出し、置換/停止の一括方針を決裁。
3. 今サイクル後: リリース前に `sitemap + 全htmlのgithub.ioリンクHEAD監査` をジョブゲートに追加（404閾値超過で alert 固定）。

## 恒久対処（優先度付き）
- **高（即時）:** URLマップ（公開先辞書）を単一ソース化し、`pharma-*`/`free-tool-*` を固定。
- **中（1-2日）:** `portal`＋記事内の相対リンク/.htmlリンクを含む総合リンク監査をCIに組み込む。
- **中（1-2日）:** `pharmacy-revenue-improvement` の公開未反映や`pharma-similar-drug-names`等古いスラグを整理し、404再発リストを潰す。

## 必要権限/環境
- sidebizワークスペース編集権限（Markdown/HTML修正）
- 該当GitHub Pagesリポジトリ更新権限（URL修正/再デプロイ）
- HTTPS疎通監視環境（cron）と`curl/urllib`系到達監視
