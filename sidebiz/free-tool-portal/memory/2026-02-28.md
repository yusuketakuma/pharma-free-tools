## 2026-02-28 (pre-compaction memory flush)

- 11:21 PM (Asia/Tokyo): `gateway(action="config.get")` で OpenClaw 設定を確認。`agents.defaults.model.primary` と `agents.list` の既存モデルは `openai-codex/gpt-5.3-codex` だった。
- `gateway config.patch` を試行したところ、先に `raw required` エラー。`raw` 文字列指定で実行し、OpenClaw設定を反映成功。
  - 変更: `agents.defaults.model.primary` を `openai-codex/gpt-5.3-codex-spark` へ変更。
  - 変更: `agents.defaults.models` に `openai-codex/gpt-5.3-codex-spark` 追加。
  - 変更: `agents.list` の `main/coding/homecare/sidebiz` の `model` を `openai-codex/gpt-5.3-codex-spark` へ変更。
  - `config.patch` 成功時に gateway 再起動 (`SIGUSR1`) 実施ログあり。
- `cron list` で `9fff...` ほか 8件のcronを確認。
- `cron update`（parallelで一括）により、全ジョブの `payload.model` を `openai-codex/gpt-5.3-codex-spark` に統一。
- `session_status` で `model` 上書き試行（`openai-codex/gpt-5.3-codex-spark`）は拒否され、現行セッションは許可された既定モデル `openai-codex/gpt-5.3-codex` が表示されることを確認（`Model ... is not allowed`）。

- 未完了メモ: 追加で `openai-codex/gpt-5.3-codex` が `agents.defaults.models` に残存するため、必要なら将来の整理対象。
