# 副業保守サイクル実行記録 - 2026-03-10 20:20 JST (sidebiz-30m-maintenance)

## status
- **alert**

## 監視結果

### 1) 起動/アクセス（サイドビズ無料ツール）
- 対象: `sitemap.xml`準拠25件（Portal含む）
- 到達性: **22件OK / 3件NG**

[ALERT] NG一覧:
- `https://yusuketakuma.github.io/pharma-medication-guidance/` → **404**
- `https://yusuketakuma.github.io/pharma-medication-reminder/` → **404**
- `https://yusuketakuma.github.io/pharma-renal-dose/` → **404**

### 2) 主要機能スモーク
- `free-tool-portal/pharmacy-ai-readiness.html`: DOM初期要素（質問描画領域・結果領域）と主要JS関数（`renderQuestions`/`nextQuestion`/`showResult`）存在を確認。
- `free-tool-medication-summary/index.html`: 生成ボタン/コピー機能周辺の主要ID/関数（`copyBtn`/`patientName`/`generateSummary`/`copyToClipboard`）存在確認。
- `free-tool-homecare-scheduler/index.html`: `homecare-patients`キー参照（`localStorage`）と保存更新処理（`localStorage.setItem`）存在を確認。

### 3) 依存・権限・認証
- 外部API実行/認証フロー（OAuth/API token）: 本巡回の取得HTML上では外部認証呼び出しの新規障害を検出せず。
- データ保存: `homecare-scheduler`は`localStorage`（`homecare-patients`）利用のみで、今回巡回で破損再現なし。
- 依存更新の状態: 本サイクルではライブラリ更新やデプロイフロー変更なし（監視中心）。

### 4) リンク監査（GitHub Pages 実URL網羅）
- 抽出リンク（対象域）: **91件**
- 404: **53件**（前回同等規模の悪化なし）

[ALERT] 404上位:
1. `https://yusuketakuma.github.io/pharma-medication-guidance/`（**22件**）
2. `https://yusuketakuma.github.io/pharma-medication-reminder/`（**21件**）
3. `https://yusuketakuma.github.io/pharma-renal-dose/`（**6件**）

加えて `yusuke-ikitama.github.io/*` 系・`free-tool-*` 系の旧/混在URLが多数参照され、ほぼ一括で404。

## 障害再現条件
1. 主要公開先に直接アクセス:
   - 上記3つの404 URLに直接遷移。
2. Portal/関連記事導線を経由:
   - `/pharma-medication-guidance/`, `/pharma-medication-reminder/`, `/pharma-renal-dose/` がリンク先として設定されたカード・関連記事を辿る。
3. `free-tool-portal/` の相対リンク導線:
   - `medication-reminder.html` や `renal-drug-dosing.html` 由来の存在しないローカルリンクをクリック。

## 影響範囲
- **高:** `pharma-medication-reminder` / `pharma-medication-guidance` / `pharma-renal-dose` が到達不可。導線起点から利用開始不能。
- **中〜高:** Portal/記事内関連記事での404誘導が多頻度。初回導線断線による離脱率上昇リスク。
- **低〜中:** `free-tool-*` / `pharma-*` /旧ドメイン混在の命名揺れ継続により、公開運用の監査コストと修正工数が増加。

## 学習ポイント（次回改善）
- URL設計が2系統以上混在すると、1回の導線修正ミスが**複数の公開導線（数十件規模）**へ同時に波及する。`公開URL辞書`を1ファイルに固定し、デプロイ前に404チェックを自動化しないと再発しやすい。
- `portal`内相対リンク（`.html`）の存在確認を監視基準に含めると、内部QAの盲点（ローカルリンク切れ）を早期に検知できる。

## 暫定対処案
- 主要3URLは暫定的に代替案内（代替URLへガイダンス）をトップに追加し、404時の復帰導線を確保。
- 旧`yusuke-ikitama.github.io/*`リンクはまず全件停止一覧化し、公開停止かURL統一かを明示。
- `homecare/scheduler`の`localStorage`破損耐性について、キー未保存時の初期化メッセージ文言を追加。

## 次アクション（30分以内で次サイクル）
1. 破損優先順位順（上位3 URL）を**即時統一**: 正式公開先にリンク置換。
2. portal相対リンク監査を追加し、リンク存在確認を事前ゲート化。
3. `Cron監視ジョブ`に「URL辞書同期チェック」「リンク404閾値超過時のアラート」を実装。

## 恒久対処（優先度付き）
- **高・すぐ実施**: URL命名規約（`pharma-*` or `free-tool-*`）を単一化し、一覧/Portal/関連リンクを一括置換。
- **中・1-2日**: リンク検証自動化（sitemap + 全HTML抽出）をCI化し、404超過時はジョブを`alert`固定。
- **低・1週以内**: 404誘導時のフォールバックUI（代替URL提示・カテゴリ再導線）を各主要画面へ追加。

## 必要権限/環境
- sidebizワークスペース編集権限（HTML/Markdown）
- GitHub Pages公開リポジトリ更新権限（URL修正反映）
- HTTPS到達監視環境（cron側）
