# 経過措置387品目 CSV・Pythonスクリプト動作検証レポート

**検証日時**: 2026-03-18 18:15 JST
**対象ファイル**:
- `drugs_54_component_delete.csv`（成分単位削除品目リスト）
- `transition-drugs-387-matcher.py`（照合スクリプト）

---

## 1. CSVデータ検証

### カラム構成

| カラム名 | 型 | 状態 |
|----------|------|------|
| 薬品コード | string | ⚠️ 全行空欄 |
| 薬品名 | string | ✅ 8行にデータあり |
| 規格 | string | ✅ |
| メーカー | string | ✅ |
| 削除タイプ | string | ✅ |
| 薬効群 | string | ✅ |
| 代替薬 | string | ✅ |
| 優先度 | string | ✅ |

### データ件数

- **期待値**: 54品目（成分単位削除対象）
- **実データ**: 8行 + プレースホルダー1行
- **充足率**: 8/54 = **14.8%**（大幅に不足）

### 登録済み品目（3成分・8規格）

| 成分 | 規格数 | 削除タイプ | 優先度 |
|------|--------|-----------|--------|
| アモキサピン（アモキサンカプセル） | 3規格（10mg/25mg/50mg） | 成分単位削除 | 高 |
| リラグルチド（ビクトーザ皮下注） | 2規格（0.5mg/1.0mg） | 成分単位削除 | 中 |
| トラニラスト（レミカットカプセル） | 2規格（100mg/200mg） | 成分単位削除 | 中 |
| テリパラチド「サワイ」 | 1規格 | 即削除（3月末） | 高 |

### 問題点

1. **[Critical] データ不足**: 54品目中8品目（+プレースホルダー行）のみ。残り46品目の追加が必要
2. **[Critical] 薬品コード未入力**: 全行で空欄 → MCS照合時にコード照合が使えない（名前照合のみ）
3. **[Minor] プレースホルダー行残存**: 最終行「（以下、厚労省PDFより追加必要）」がデータとして読み込まれる
4. **[Info] テリパラチド「サワイ」**: 「即削除（3月末）」= 2026-03-31 → **残り13日**。既に対応済みか要確認

### フレームワークとの関係

- フレームワーク（`keika-sochi-homecare-analysis-framework.md`）のカテゴリA成分（リラグルチド・アモキサピン・トラニラスト）は**全てCSVに登録済み** ✅
- カテゴリB（「54品目リストのうちカテゴリA以外」）: 前回フィードバックで「54品目と387品目全体との関係を明記」と指摘あり → CSVデータが不足しているため、カテゴリB品目の具体化が困難
- **387品目全体のCSV** (`drugs_387_list.csv`): **未作成** → スクリプトのmain()実行時にエラー

---

## 2. Pythonスクリプト検証

### 構文チェック
- Pythonバージョン: 3.x互換 ✅
- 構文エラー: なし ✅
- 依存ライブラリ: csv, json, pathlib, typing（全て標準ライブラリ）✅

### 関数一覧と動作確認

| 関数名 | 目的 | テスト結果 |
|--------|------|-----------|
| `load_prescriptions()` | MCSエクスポートCSV読み込み | ✅ 正常動作 |
| `load_drugs_387()` | 387品目リスト読み込み → Set+Dict | ✅ 正常動作 |
| `load_component_delete()` | 54品目リスト読み込み | ✅ 正常動作（FileNotFoundも適切にハンドル） |
| `match_prescriptions()` | 処方データと387品目を照合 | ✅ 正常動作 |
| `save_result()` | JSON出力 | ✅ 正常動作 |
| `print_summary()` | コンソール表示 | ✅ 正常動作 |
| `main()` | メイン処理 | ⚠️ データファイル未準備のため実行不可（設計通り） |

### モックデータでのテスト結果

テストデータ4件（マッチ3件+非マッチ1件）で検証:

```
Match Results:
  Total prescriptions: 4
  Matched: 3
  Patients affected: 2
  High priority: 2
  Patient P001 (山田太郎): [アモキサンカプセル25mg, レミカットカプセル200mg]
    -> Component delete: [アモキサンカプセル25mg, レミカットカプセル200mg]
  Patient P002 (鈴木花子): [ビクトーザ皮下注1.0mg]
    -> Component delete: [ビクトーザ皮下注1.0mg]
```

照合ロジック・患者別集計・優先度分類すべて正常。

### スクリプトの改善提案

1. **[推奨] 部分一致マッチング追加**: 現状は完全一致のみ。MCSの薬品名表記揺れ（スペース・全半角等）に対応するため、正規化処理の追加が望ましい
2. **[推奨] CSV出力オプション**: JSON出力に加えてCSV出力を追加（Excel利用者向け）
3. **[推奨] フレームワーク連携**: カテゴリ分類（A〜E）の自動判定機能を追加
4. **[Info] 実行パス**: ファイルパスがカレントディレクトリ相対 → 絶対パスまたはコマンドライン引数対応が望ましい

---

## 3. 次のアクション

### ゆうすけ対応待ち

1. **MCSデータエクスポート** → `prescriptions_export.csv` の作成
2. エクスポート後、スクリプトの `drug_name_col` をMCSのカラム名に合わせて調整

### 自律実行可能

1. **CSVデータ拡充**: 厚労省PDF（`r8-transition-drugs-list-387.pdf`）から残り46品目を抽出・追加
2. **プレースホルダー行削除**: 最終行の「（以下、厚労省PDFより追加必要）」を除去
3. **387品目全体CSV作成**: `drugs_387_list.csv` を厚労省PDFから作成
4. **スクリプト改善**: 名前正規化・CSV出力・カテゴリ自動分類の追加

### 優先度判断

CSV拡充（Action 1-3）はMCS抽出を待たずに実行可能。ただし387品目PDFの読み取り精度に注意が必要（PDF→テキスト変換の精度検証を含む）。次回実行サイクルでの着手を推奨。
