# 2026-03-24

- OpenClaw agents default model was changed to `openai-codex/gpt-5.4-mini`, while `ceo-tama`, `supervisor-core`, and `dss-manager` remain on `openai-codex/gpt-5.4` for heavier judgment and coding-oriented work.
- After upgrading to OpenClaw `2026.3.22`, core functions (CLI, Gateway, Telegram, browser control) were verified working, but Control UI assets were missing from the npm-distributed package; `pnpm ui:build` could not be completed because the package lacks `scripts/ui.js`. Decision: operate without Control UI until upstream packaging improves.
- `dss-manager` is configured and has auth files present, but as of this date it has no sessions/activity yet; status is effectively registered-but-not-yet-started.
- User wants ongoing research workflows to shift from pure continuation into a pattern of `continue -> produce artifact -> integrate`, so recurring research should yield concrete outputs and then unified summaries.
- Notification/report policy remains: normal outputs aggregate into regular reports rather than direct notification, unless emergency.
- User explicitly wants the autonomous improvement loop strengthened through agent evaluation, lesson capture, staffing optimization, and authority for the supervisor layer to add new specialized agents when a frequently repeated workflow justifies it.
- Implemented recurring supervisor cron jobs for: agent scorecards, lesson capture, staffing/prompt tuning, workforce expansion review, and domain specialization review; these are daily morning isolated agent turns with delivery mode `none`.
