# sidebiz-30m-maintenance (2026-03-11 20:52 JST)

[ALERT] 主要導線3件（guidance / renal-dose / reminder）の404が継続

- status: **alert**

## 監視結果

### 起動/アクセス（監視25）
- **22OK / 3NG（major）**
- NG（major）:
  - `https://yusuketakuma.github.io/pharma-medication-guidance/`
  - `https://yusuketakuma.github.io/pharma-renal-dose/`
  - `https://yusuketakuma.github.io/pharma-medication-reminder/`

### 主要機能 smoke
- `pharma_free_tools`: **OK**（到達）
- `medication_summary`: **OK**（到達）
- `homecare_scheduler`: **OK**（到達）
- 代替導線確認:
  - `pharma-medication-guidance-tool`: **OK**（到達）
  - `pharma-renal-dose-tool`: **OK**（到達）
  - `pharma-medication-calendar`: **OK**（到達）

### 依存・保存・認証
- 依存外部（GitHub Pages）の到達自体は一部URLで200を継続。認証エラー(401/403)は未検知。
- 今回監査では DB/ローカル保存の障害、クレデンシャル更新、権限失効は確認対象外（到達監査中心）。

## 障害再現条件

1. 対象3URLをブラウザ/HTTP(GET)で直接開くと即時 `404`。
2. `sidebiz/free-tool-portal/sidebiz/free-tool-medication-reminder/index.html` の旧slug導線（guidance/renal-dose/reminder）からも同条件で遷移不可。
3. `sidebiz/` 配下の `sitemap.xml` に旧slugが残存。
4. 代替導線は `pharma-guidance-tool / renal-dose-tool / medication-calendar` で到達可能。

## 影響範囲

- **高**: 主要到達導線3件停止（`guidance` / `renal-dose` / `reminder`）。
- **中**: `sitemap.xml`と1本の関連記事ページで旧slug参照が残り、検索・既存経路からの再訪導線の離脱リスクあり。
- **低**: コア機能群（free-tools/summary/scheduler/薬歴等）の起動は継続。

## 学習ポイント（次サイクル反映）

1. **公開URL名変更は、公開方針確定→旧slug導線監査→同一サイクル反映**をセットで回すと再発を抑えやすい。
2. 運用監視では、**到達性エラー数だけでなく、旧slug残存箇所（sitemap含む）を横断監査**することで、影響範囲の説明速度が向上する。

## 次アクション

### 30分
1. 重大3導線の復旧方針（公開継続or代替ページ化）を trainerで明文化し即時決裁。
2. 代替導線（guidance-tool / renal-dose-tool / medication-calendar）へ統一的に誘導する暫定文言を適用。
3. `sitemap.xml` と旧slug掲載ページの即時差分修正（影響回避を優先）。

### 2時間
4. 旧slug参照監査（`sitemap + med-reminder`）を完了報告し、未解消分を開発タスク化。
5. major/noiseルールを継続し、majorのみALERTエスカレーション、noiseは2h集約での報告を維持。

## 必要権限/環境

- GitHub Pages編集・再デプロイ権限（必要時）
- `guidance / renal-dose / reminder` の最終公開方針決裁
- 監視環境: HTTPS到達監視（curl/HTTP GET）とリンク監査パイプライン
- 社外報告は `trainer-2h-regular-report` に集約（本ジョブ内で sidebiz/homecare/trainer チャネルへ即時投稿なし）

- Evidence: `memory/sidebiz-30m-check-result-live.json`（checked_at_utc: 2026-03-11T11:54:29.745Z）