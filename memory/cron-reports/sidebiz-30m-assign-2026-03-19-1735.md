# sidebiz-30m-assign 2026-03-19 17:35

**status**: done

## 開発内容
1. 直近の収益化ボトルネックを再計測
   - ルートHTML 73、CTA設置 72、GA4プレースホルダー残 72、OGP 73 を確認
   - ニュースレター事前導線は `index.html` に実装済み（`newsletter_intent` あり）
   - 主阻害を「GA4実Measurement ID未反映」と「外部アカウント依存（Buttondown/Vercel）」に再整理
2. CTAカバレッジを拡大
   - `scripts/add_cta_batch_v2.py` を追加
   - `cta-section` 未付与の32ページへCTAを追加し、全ツールHTML（index除く72/72）でCTA存在を確認
3. 検証基準の不整合を修正
   - `scripts/verify-portal-integrity.sh` のCTA判定を `index.html` 除外 + `cta-section/cta_click` 基準へ変更
   - `sidebiz/kpi-tracker.md` の実測サマリーを CTA72 に更新
4. 品質事故の即時是正
   - 原因: 旧形式CTA（classなし）を既存ページにも追加してしまい、31ファイルで重複発生
   - 対応: `scripts/dedupe_cta_sections.py` を追加し、旧形式のみ除去
   - 影響範囲: ローカルHTML差分のみ、未配信

## 検証結果
- `./scripts/verify-portal-integrity.sh`
  - HTML: 73
  - CTA: 72
  - OGP: 73
  - GA4プレースホルダー: 72
  - KPIトラッカー整合: ✅
- サンプル確認
  - `patient-identification-checklist.html` のCTA重複解消を確認
  - `pharmacist-quiz-generator.html` に `cta-section` + `cta_click` を確認

## 学習ポイント（次サイクル反映）
1. 一括改変は「存在判定ルール」と「検証ルール」を同じキー（今回は `cta-section`）へ揃えてから実行する
2. 旧形式互換を残す場合でも、追加後に「重複ブロック数」チェックを必須化する
3. 副業担当の評価軸は、件数ではなく「実測一致まで直した本番反映準備件数」を優先する

## 保守引き継ぎ
- 再現手順
  - `./scripts/verify-portal-integrity.sh`
  - `grep -l 'cta-section' *.html | wc -l`
- 想定リスク
  - GA4は72ページでプレースホルダー残存のため、本番計測は未開始
  - Buttondown未作成のため、ニュースレターは意図計測のみ
  - Vercel 404は継続、公開導線はGitHub Pages前提で監視継続
- 依存
  - GA4実Measurement ID
  - Buttondown等メール配信アカウント
  - Vercelダッシュボード操作
- 監視観点
  - CTA数（72維持）
  - GA4プレースホルダー残数（72→0）
  - `newsletter_intent` / `cta_click` イベント送信可否
  - Vercel 404継続有無

## 次30分アクション
1. GA4実Measurement ID差し替え手順を1枚化し、ID判明後の即時実行準備を完了する
2. Buttondown未接続でも壊れない形で、`templates/email-capture-form.html` の埋め込みドライラン範囲を限定して確認する
3. Vercel 404の運用影響を減らすため、GitHub Pages優先導線の明文化案を整理する

**通知抑止**: 本報告は内部整理のみ、trainer-2h-regular-reportへ集約
