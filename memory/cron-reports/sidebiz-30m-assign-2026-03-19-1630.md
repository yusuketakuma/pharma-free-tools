### 16:30 - 30分開発 (sidebiz-30m-assign)

**status**: done

**開発内容**:
1. **再発ボトルネック調査**
   - 収益化の直近ボトルネックは「販売アカウント待ち」そのものではなく、**販売導線の未配置ページが24件残っていたこと**と確認
   - 実測: ルートHTML 73件、CTA設置 40→64件、GA4プレースホルダー 72件残
   - `scripts/verify-portal-integrity.sh` を作成し、HTML数/CTA数/GA4/OGPを固定コマンドで実測できる状態へ整理

2. **実装内容**
   - `scripts/replace-ga4-id.sh` 作成
     - GA4 Measurement IDが判明後、72ページを対話付きで一括置換できるよう準備
   - `scripts/add_cta_batch.py` 作成
     - CTA未設置24ページへ `ai-prompts-lp.html` 導線を安全に追記
   - `scripts/verify-portal-integrity.sh` 作成
     - dashboard-data.json 自動更新を含む実測スクリプト化

3. **簡易検証結果**
   - CTA未設置ページ: 24 → 0
   - 実測結果: HTML 73 / CTA 64 / OGP 73 / GA4プレースホルダー 72
   - サンプル検証: `antihypertensive-selector.html` に CTA Section 挿入を確認

**学習ポイント（次サイクル反映）**:
1. CTA追加のような複数HTML改変は awk の埋め込み文字列より Python 差し込みの方が再現性が高い → 次回もHTML一括改変は Python優先
2. 検証指標は「スクリプト成功ログ」ではなく「再スキャン結果」を正とする → 次回も `verify-portal-integrity.sh` 実測で完了判定
3. gitバックアップ時に `git add -A` はワークスペース全体の無関係変更を巻き込みやすい → 次回は対象ファイル限定 add に変更

**失敗・阻害・品質事故**:
- 品質事故: 初回 `add-cta-batch-v2.sh` は awk の multiline 埋め込みで警告を出しつつ成功表示した
- 原因仮説: awk 文字列に改行を直接持たせた実装が macOS awk と相性不良
- 再発防止策: HTML挿入処理は Python に統一、成功判定は再スキャンで確認
- 影響範囲: ローカルワークスペース内のみ、公開反映・外部通知なし

**保守担当への引き継ぎ**:
- 想定リスク:
  - CTA文言が各ページ文脈に最適化されていない（汎用文言）
  - GA4は依然プレースホルダーのため計測精度未完成
  - `git add -A` による無関係変更コミットがローカルに1件あるため、push前に差分精査が必要
- 依存:
  - GA4実Measurement ID（ゆうすけ依存）
  - Payhip等の販売アカウント作成（ゆうすけ依存）
- 監視観点:
  - `./scripts/verify-portal-integrity.sh` の CTA数/GA4残数
  - CTA追加ページの表示崩れ有無
  - `ai-prompts-lp.html` への遷移クリック計測（GA4反映後）

**次30分アクション**:
1. `sidebiz/kpi-tracker.md` を実測ベース（73ツール/CTA64）へ更新し、手動台帳依存を縮小
2. CTA追加24ページから優先5ページを選び、文脈別CTA文言へ改善
3. GA4実ID未着でも進められる「購入前メール導線 or 問い合わせ導線」の1ページPoCを追加検討

**通知抑止**: 本報告は内部整理のみ、trainer-2h-regular-reportへ集約
