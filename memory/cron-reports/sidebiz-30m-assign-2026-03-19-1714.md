# sidebiz-30m-assign 2026-03-19 17:14

**status**: done

## 開発内容
1. `index.html` にニュースレター事前導線バナーを追加
   - Buttondown未接続でも壊れない暫定導線
   - 当面はAIプロンプトLPへの先行案内へ遷移
   - `newsletter_intent` GA4イベントを追加
2. `sidebiz/kpi-tracker.md` 冒頭へ実測サマリーを追加
   - ツール総数73 / CTA64 / OGP73 / GA4プレースホルダー72 / sitemap87
   - `scripts/verify-portal-integrity.sh` と整合回復

## 検証結果
- `grep` でニュースレター導線挿入を確認
- `./scripts/verify-portal-integrity.sh` 再実行で KPIトラッカー整合 ✅
- 残課題は GA4プレースホルダー72件

## 学習ポイント
1. 外部アカウント待ちでも、壊れない暫定導線 + 計測追加でPoC化可能
2. KPI台帳は機械可読サマリーを別置きすると監視の誤検知を減らせる
3. zsh検証コマンドでは `status` でなく `rc` を使う

## 保守引き継ぎ
- 再現手順:
  - `grep -n "newsletter_intent\|業務改善ニュースレター" index.html`
  - `./scripts/verify-portal-integrity.sh`
- リスク:
  - 現在はメール収集開始前の事前導線
- 依存:
  - Buttondown等のメール配信アカウント
  - GA4実Measurement ID
- 監視:
  - `newsletter_intent` クリック数
  - GA4プレースホルダー残数
  - KPIトラッカー整合

## 次30分アクション
1. Buttondown接続前提で `templates/email-capture-form.html` を indexへ本実装化
2. GA4実Measurement ID判明後に72ページ一括置換
3. CTA追加済みページの文脈別文言改善（5ページ）

**通知抑止**: 本報告は内部整理のみ、trainer-2h-regular-reportへ集約
