### 18:12 - 30分開発 (sidebiz-30m-assign)

**status**: alert

**[ALERT] 実装ボトルネックは Vercel 404 だけでなく、`root repo` と `sidebiz/free-tool-portal` の二重管理による誤検証リスク**

**開発内容**:
1. **ボトルネック調査・再現**
   - root repo (`/Users/yusuke/.openclaw/workspace`) と nested repo (`sidebiz/free-tool-portal`) の HEAD が乖離
   - root `index.html` には `newsletter_intent` が存在
   - nested `index.html` には存在せず、こちらを見た保守確認は「未実装」と誤判定する状態
   - GitHub Pages は 200、Vercel は 404 を再確認

2. **実装**
   - `scripts/verify-portal-integrity.sh` を強化
     - Source of Truth 表示を追加
     - GA4実IDカウントの誤判定修正（placeholder除外）
     - `index.html` のニュースレター導線確認を追加
     - nested repo 乖離検出を追加
     - `dashboard-data.json` に `newsletterIndex` / repo情報を追加
   - `sidebiz/portal-source-of-truth.md` を新規作成
     - 正本パス、誤検証の原因、標準検証コマンド、保守ルールを整理

3. **検証結果**
   - `bash scripts/verify-portal-integrity.sh`
     - HTML 73
     - CTA 72
     - OGP 73
     - GA4 placeholder 72
     - GA4 live 0
     - newsletter index 1
     - nested repo status: diverged
   - 実測により「ニュースレター暫定導線は root には存在、nested には不在」を確認

**学習ポイント（次サイクル反映）**:
1. 完了判定は「正本repo固定」なしでは壊れる。次サイクルも root 実測を正とする
2. 検証スクリプトは指標数だけでなく「repo乖離」も監視しないと誤報を防げない
3. GA4 live 判定は placeholder 除外ロジック必須。次回以降も実ID置換後に同じ基準で確認する

**失敗・阻害・品質事故**:
- 原因仮説: 古い nested copy を確認した結果、実装済み/未実装の認識が混線
- 再発防止策:
  1. `portal-source-of-truth.md` を保守引き継ぎに使用
  2. `verify-portal-integrity.sh` を root 基準の標準コマンドに固定
  3. 報告テンプレートへ `repo相対パス + grep根拠 + file existence` を必須化
- 影響範囲: ニュースレター導線・検証スクリプトの存在判定、保守レポートの信頼性

**保守担当への引き継ぎ**:
- 想定リスク:
  1. nested repo を見続けると未実装誤報が再発
  2. Vercel 404 は未解消のため公開導線は GitHub Pages 優先
  3. GA4実ID未反映のためコンバージョン計測はまだ開始不可
- 依存:
  1. Vercelダッシュボード操作（ゆうすけ）
  2. GA4 Measurement ID 実値（ゆうすけ）
  3. Buttondownアカウント作成
- 監視観点:
  1. `bash scripts/verify-portal-integrity.sh` の `newsletter index` / `nested repo status`
  2. GitHub Pages 200 / Vercel 404 の継続状況
  3. GA4 placeholder 残数

**次30分アクション**:
1. 【高】Vercel再デプロイ依存点を `root repo` 基準で1枚化
2. 【高】GA4実ID差替時の検証手順を `verify-portal-integrity.sh` 前提で固定化
3. 【中】Buttondown未作成でも壊れないメール導線の本実装条件を整理

**通知抑止**: 本報告は内部整理のみ、trainer-2h-regular-reportへ集約
