# 2026-03-26

- dominant-prefix telemetry should route to a dedicated `Queue Triage Analyst` path; repeated `supervisor-core` review loops are too repetitive.
- `heartbeat` / `board` / `scorecard` should behave like anomaly-delta monitors: routine periods emit digest, anomalies emit candidate, deep review only when root policy changes.
- stale `waiting_auth` / `waiting_manual_review` backlog after auth recovery should be handled as a board-routed safe-close / reopen problem, with runtime queue state kept read-only until the decision lands.
- workspace ↔ live runtime reflection is unsafe when done file-by-file; `run_claude_acp.py` / `run_claude_code.sh` / `execute_task.py` need bundle manifest + dry-run sync as a unit.
- sidebiz scouting continues to value 1-entry-point comparison with `owner / due / success criteria`; current top candidate from the latest scout is reservation / no-show support, followed by inquiry / estimate follow-up and invoice / payment reconciliation.
- `trainer/` had no new updates after 2026-03-21, so the new learning source is effectively `reports/` + `projects/openclaw-core/`.
- handoff preflight should require `owner / due / success criteria` on queue triage and scout handoffs; this is the lowest-risk lever to reduce retry cost.
