# 2026-03-22

- OpenClaw設定を更新し、メイン `ceo-tama` から委任可能な専門エージェント `research-analyst` / `github-operator` / `ops-automator` / `doc-editor` を追加。複数エージェント協働可能な構成に変更し、gateway再起動まで実施。
- 旧OpenClaw運用の残骸を整理。現行のcron jobsは0件で、旧通知の主因候補は旧エージェント群のセッション/ログだったため、`homecare` / `sidebiz` / `trainer` / `main` / `unified` / `coding` / `department-heads` / `logical-specialists` の agent ディレクトリを削除。`ceo-tama` / `claude-code` / `control-plane` は残置。
- ユーザー依頼で gateway restart を実施（2026-03-22 夜）。
- `pharma-free-tools` の現状確認:
  - 実体リポジトリは `/Users/yusuke/pharma-free-tools`、OpenClaw側の管理プロジェクトは `projects/pharma-free-tools`。
  - 実リポジトリ `main` の最新確認時点では未追跡ファイルが残存（印刷用HTML/CSS、product関連README、Payhip/X告知下書き等）。
  - 実リポジトリでは `html_files=74` に対し `index.html` の `tool-card` 数と `sitemap.xml` URL数が各87で、管理メタ情報との数値ズレがあるため、次にやるべきは新規開発より「ツール数・index・sitemap・dashboard-data.json の整合取り」と判断。
  - `vercel.json` は実リポジトリに存在し、以前の Vercel 設定不足は少なくともファイル上は解消済み。
  - GA4タグは広く埋め込まれているが、実ID差し替え未完了のメモが残っており、プレースホルダ運用が継続している可能性が高い。
- `pharma-free-tools` の方針転換を反映:
  - 旧方針「毎日1本新規ツール追加」を停止し、「薬局・薬剤師の日常業務の実需リサーチ→高ニーズだけ開発」へ変更。
  - 初回リサーチの上位候補は `薬歴記載時短` / `返戻再請求の実行支援` / `在庫発注・不動在庫・欠品対応`。
  - 既存の診断・チェックリストを増やすより、現場でそのまま使える実行ツールへ改善する方針に整理。
  - `projects/pharma-free-tools/docs/status.md` / `backlog/queue.md` / `learn/improvement-ledger.md` を新方針に更新。
